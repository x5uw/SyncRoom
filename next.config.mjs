/**
 * next.config.mjs
 *
 * Next.js configuration file using ESModule syntax. This file exports
 * the NextConfig object which allows you to customize various Next.js
 * build and runtime behaviors (e.g., rewrites, redirects, environment
 * variables, webpack tweaks, etc.).
 *
 * The `@type` JSDoc annotation references Next.js's built-in NextConfig
 * type definitions, enabling IDEs like VSCode to provide IntelliSense
 * and type-checking even in plain JavaScript projects.
 */

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
